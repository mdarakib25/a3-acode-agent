# Acode Antigravity Agent (A3)

Acode-এর জন্য একটি স্বয়ংক্রিয় এআই এজেন্ট প্লাগইন — Google Antigravity IDE ও Windsurf-এর কার্যপ্রণালী দ্বারা অনুপ্রাণিত।

## বর্তমান অবস্থা (v0.16.1)

সব মূল ফেজ (Phase 1–11, Phase 11 আংশিক) + একটা বড় UI/UX ওভারহল সম্পন্ন।

## ফাইল স্ট্রাকচার

- `main.js` — এন্ট্রি পয়েন্ট, হেডার (নতুন চ্যাট/হিস্টোরি/সেটিংস), চ্যাট UI,
  ফাইল অ্যাটাচমেন্ট, ওয়েলকাম স্ক্রিন, টাস্ক চেকলিস্ট, Diff/Command/Plan কার্ড
- `src/storage.js` — প্রোভাইডার কনফিগ সেভ/লোড (localStorage)
- `src/settings.js` — প্রোভাইডার কনফিগারেশন ভিউ (মাল্টি-স্লট)
- `src/sessionStore.js` — **মাল্টি-সেশন চ্যাট হিস্টোরি**, localStorage-ভিত্তিক
  (provider config-এর মতোই প্যাটার্নে) — প্রতিটা চ্যাট আলাদা সেশন হিসেবে সেভ হয়
- `src/apiEngine.js` — Universal API Engine + নেটিভ function-calling +
  Stop বাটনের জন্য AbortSignal সাপোর্ট
- `src/agentTools.js` — ফাইল/টার্মিনাল টুলস + `savePlanFile`, `openFileInEditor`
- `src/agentLoop.js` — এজেন্ট লুপ: প্ল্যান Allow/Deny, টাস্ক-স্ট্যাটাস ট্র্যাকিং,
  ফাইল অটো-ওপেন, abort-সাপোর্ট
- `src/diffUtil.js` — লাইন-বাই-লাইন LCS diff অ্যালগরিদম
- `style.css` — সব UI এলিমেন্টের স্টাইল

## v0.13.0-এর বড় পরিবর্তনগুলো

1. **ইউজার মেসেজ বাবলের রঙ** বদলানো হয়েছে (বেগুনি)
2. **মাল্টি-সেশন চ্যাট** — হেডারে +/History/Settings; হিস্টোরি এখন
   localStorage-এ (provider config-এর প্যাটার্নে), ফাইলে না
3. **সবসময় Agent Mode** — আলাদা টগল/এরর-ফিক্স বাটন সরানো হয়েছে
4. **Send/Stop বাটন** — চলমান রিকোয়েস্ট থামানো যায় (AbortController)
5. **ফাইল অ্যাটাচমেন্ট** — 📎 দিয়ে প্রজেক্টের যেকোনো ফাইল কনটেক্সটে যোগ করা যায়
6. **ওয়েলকাম স্ক্রিন** — খালি চ্যাটে রেডিমেড প্রম্পট দেখায়
7. **প্ল্যান Allow/Deny + টাস্ক চেকলিস্ট** — প্ল্যান এখন বাধ্যতামূলক অনুমোদন
   লাগে, `.a3/plans/*.md`-এ সেভ হয়, প্রতিটা ধাপের প্রগ্রেস (⬜/⏳/✅) দেখা যায়
8. **ফাইল অটো-ওপেন** — এডিট/তৈরি হওয়া ফাইল এডিটরে সাথে সাথে খোলে
9. **লাইন-বাই-লাইন Diff** — লাল/সবুজ হাইলাইট, বড় ফাইলে শুধু বদলানো অংশ +
   অল্প context দেখায়, পুরো ফাইল স্ক্রল করা লাগে না
10. **সেটিংস UI/UX পলিশ** — ভালো স্পেসিং, কার্ড-স্টাইল, ফোকাস স্টেট

## v0.14.0-এর সংযোজন (Rutex থেকে অনুপ্রাণিত, কোড পড়ে যাচাই করে)

1. `rename_path`/`move_path` টুল যোগ
2. URI/পাথ হ্যান্ডলিং সরলীকরণ — সব জায়গায় একই `normalizeRelativePath()` +
   `Url.join()` প্যাটার্ন, কখনো ম্যানুয়াল URI রূপান্তরের চেষ্টা না
3. চ্যাট হিস্টোরি + এডিট হিস্টোরি এখন IndexedDB-তে (আগে localStorage/ফাইল)
4. এডিটের পর self-verify করার system-prompt নির্দেশ
5. `view_edit_history` টুল — সেশনের সব ফাইল-পরিবর্তনের অডিট ট্রেইল
6. ইনপুট ড্রাফ্ট অটো-সেভ (প্রতি-সেশন)
7. `read_file_content`-এ ঐচ্ছিক `start_line`/`end_line`
8. স্ট্যাটাস মেসেজে বৈচিত্র্য (STATUS_PHRASES)
9. TRACE IMPORTS নির্দেশনা

## জানা সীমাবদ্ধতা (আপডেটেড)

- টাস্ক চেকলিস্ট (`currentTasks`) সেশনের সাথে persist হয় না — প্যানেল
  রি-মাউন্ট বা সেশন সুইচে চেকলিস্ট রিসেট হয়ে যায় (শুধু চ্যাট মেসেজ
  localStorage-এ থাকে, টাস্ক প্রগ্রেস না)
- "বড় কাজ" কখন প্ল্যান করা উচিত সেটা LLM নিজে বিচার করে (system prompt
  guidance) — কোনো প্রোগ্রাম্যাটিক নিশ্চয়তা নেই যে প্রতিবার সঠিকভাবে
  সহজ/জটিল আলাদা করবে
- ফাইল অ্যাটাচমেন্ট শুধু টেক্সট ফাইলের জন্য কাজ করবে (readFileContent-এর
  উপর নির্ভরশীল); বাইনারি ফাইল (ছবি ইত্যাদি) অ্যাটাচ করলে এরর/গার্বেজ আসতে পারে
- `editorManager.addNewFile()` কনফার্মড API হলেও বাস্তবে সব Acode ভার্সনে
  একইভাবে আচরণ করে কিনা পুরোপুরি টেস্ট করা হয়নি — অটো-ওপেন কাজ না করলে
  জানাবেন
- Stop বাটন শুধু API কল (fetch) থামায়; কোনো run_command ইতিমধ্যে চলতে
  শুরু করলে (Executor.execute) সেটা থামানোর কোনো নিশ্চিত উপায় নেই, তাই
  Stop চাপলেও ব্যাকগ্রাউন্ডে টার্মিনাল কমান্ড চলতে থাকতে পারে
- আগের ফেজ-ভিত্তিক (Phase 1-11) সব ফিচার ও সীমাবদ্ধতা অক্ষুণ্ণ আছে —
  পুরনো changelog এন্ট্রিতে বিস্তারিত দেখুন

## ডেভেলপমেন্ট নোট

বান্ডলার ছাড়া plain JS, `<script>` ট্যাগে ক্রমানুসারে ফাইল লোড হয়।
